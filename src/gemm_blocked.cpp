#include "gemm.hpp"

void gemm_blocked(const float* A, const float* B, float* C, size_t M, size_t N,
                  size_t K) {
  // TODO: implement cache-blocked version
  (void)A;
  (void)B;
  (void)C;
  (void)M;
  (void)N;
  (void)K;  // Suppress unused warnings
}
