#include "gemm.hpp"

void gemm_simd(const float* A, const float* B, float* C, size_t M, size_t N,
               size_t K) {
  // TODO: implement NEON SIMD version
  (void)A;
  (void)B;
  (void)C;
  (void)M;
  (void)N;
  (void)K;
}